
# Lecture Data batch (B)

# lecture data éclairage (L)

# Calcul de X(w,j), Y(w,j), Z(w,j) pour chaque éclairage (indépendant de l'objet)

# Calcul pour chaque pair (B,L)
    # du X(i,j), Y(i,j), Z(i,j)

    # du L*(i,j) a*(i,j) b*(i,j)

# Recherche d'une paire d'éclairage tel que deux batch couleurs aient une distante Lab petite (tend vers 0) sur un éclairage et grande sur un autre

# Extraction des RGB des deux batch et de l'indice des deux éclairages

# génération d'une image au ratio feuille A5 avec le texte "PINTO_LUTTMANN" au centre de la couleur d'un batch, et le fond à la couleru de l'autre batch

# enregistrer l'image au format png